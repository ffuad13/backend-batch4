meta {
  name: Login User
  type: http
  seq: 5
}

post {
  url: http://localhost:4000/api/user/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "Malik",
    "password": "Password123!"
  }
}

settings {
  encodeUrl: true
}
